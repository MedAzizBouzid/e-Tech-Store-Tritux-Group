<!DOCTYPE html>

<title>Edit Order</title>

{% block body %}
    <h1>Edit Order</h1>

    {{ include('back/order/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_order_index') }}">back to list</a>

    {{ include('back/order/_delete_form.html.twig') }}
{% endblock %}
