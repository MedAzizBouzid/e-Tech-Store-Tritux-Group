<!DOCTYPE html>

<title>Edit Media</title>

{% block body %}
    <h1>Edit Media</h1>

    {{ include('back/media/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_media_index') }}">back to list</a>

    {{ include('back/media/_delete_form.html.twig') }}
{% endblock %}
